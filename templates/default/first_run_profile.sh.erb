#!/bin/bash

echo 'Workstation first run. Press <ENTER>.'
read
find ~/.firstrun -type f -name "*.sh" -exec bash {} \;
sudo rm -- /etc/profile.d/firstrun.sh

echo 'Firstrun setup done. Please relog to finalize changes'